package com.alphadocuments.documentorganiserbackend.entity.enums;

/**
 * Types of principals that can be granted permissions.
 */
public enum PrincipalType {
    USER,       // Individual user
    ROLE,       // Workspace role
    GROUP       // User group (future)
}
